# 报表组终端脚本

## 目标
提供不依赖浏览器的命令行入口，选择 Excel 和工作表后按报表组步骤处理。

## 步骤
1. 新增终端脚本，复用现有 `report_processor.process_report_step`。
2. 启动时列出工作表，允许输入序号。
3. 每步显示进度、输出文件路径，并等待用户确认后继续。
4. 增加参数模式，支持直接传入文件和工作表。
5. 运行语法检查和命令行小样本验证，记录版本。
