# 赵淑华报销凭证图片识别

## 目标

赵淑华的“发票识别”在原有“付款截图、水单”表头之外，识别“报销凭证”列对应的嵌入图片。

## 实施步骤

1. 将“报销凭证”加入支付图片表头匹配词。
2. 对合并表头读取完整的跨列范围，并将覆盖的每列均作为图片列。
3. 保持既有的图片提取范围、OCR 识别及从 R 列回写规则不变。
4. 使用模拟工作簿验证该表头能被发现，并进行 Python 语法检查。
