D:\caiwu\python-Excel  在这个文件夹里里，，写一个脚本，功能是将多个表和并成一个汇总表，然后可以用粘贴板将要合并的表格粘贴到终端里，参考D:\caiwu\pdf-v-photo\pdf转图片.py这个脚本是怎么实现粘贴表格进来的，然后也要支持多个表格一起粘贴，然后回车，然后将全部的表的名称合并去重之后，把全部的列名都列出来，然后给选择，选择之后回车，然后把哪些表只取这些已选择的列的数据，然后进行追加合并，表数据的追加顺序就按照终端里哪些表的顺序来，然后追加合并之后，在对应的表数据后面新增一列该数据原来的数据源的表名称这样后续能知道什么数据来自什么表，然后合并成一个从表之后，进行透视汇总，列名给选择，自定义列名还是固定的，自定义就把这个合并好的汇总表的全部的列列出来给选择，固定模式的话，就用这些列做透视：运单号、币种、求和项:运费、附加费2（如超围长重附加费）、附加费1（如偏远费）、附加费3（如超长重附加费）、报关费、合计金额。然后透视表做好之后，给提示就是这个透视表要直接输出保存还是插入表的表里的，要插入表格的话，提示粘贴要插入的主表，粘贴之后回车，把透视表插到那个粘贴的主表里做工作表，然后给提示输入该透视表的名称，等待输入之后会车确定，然后列出这个主表的全部工作表然后等选择，选择之后回车，然后在这个已选择工作表的表里用”运单号“去透视表里的”运单号“找到透视表里的”运费“这一列，返回到主表，然后让在最后一列，然后再“运费”这列后面再新增一列，用这个”运费减去主表里已有的“运费”这列数据。