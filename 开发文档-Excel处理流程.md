# Excel 五表联动处理脚本开发文档

## 1. 文档目的

本文档用于固化当前已确认的业务处理规则，作为后续 Python 脚本开发、联调和维护的唯一执行依据。

本次任务不是一次性清洗单个文件，而是开发一个带终端交互的脚本，按固定顺序接收多个 Excel 文件路径，并依次完成 5 个表的联动处理。

## 2. 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0.0 | 2026-04-29 | 初版需求理解文档，已纳入客户代码与业务员的重复检测完整流程。 |
| v1.1.0 | 2026-08-05 | 修复超大表处理崩溃：新增 `CHUNK_ROWS=20000` 分块读写（`iter_range_rows_in_chunks`、`write_rows_in_chunks`、`clear_range_in_chunks`），模式 1 综合数据明细写入与模式 2 数据覆盖改为分块读写。 |
| v1.2.0 | 2026-08-05 | **修复崩溃真正根因**：数据源中存在以“=”开头的文本单元格（如 `=844元 @Luo`、`=FBA19HX22PYG`，见源表 8792/110742/269307 行）。这些文本通过 COM 写入时被 Excel 当作公式解析——非法公式直接抛错（0x8007000E），合法公式被解析成错误值写坏数据。修复：新增 `excel_safe_value`，对所有以“=”开头的字符串前置单引号，统一在 `as_row_matrix`/`as_column_matrix` 中生效，保证按纯文本原样写入。分块本身并不能解决该问题（已通过二分验证）。 |
| v1.3.0 | 2026-08-08 | 新增模式 4「工作表处理」：拖入一个表格后列出全部工作表供选择，再二选一（① 复制全部数据并原位粘贴为数值，清除公式且保留格式；② 列出第 1 行列名，选择要保留的列，删除其余列）。升级模式 3「合并表格」：每个文件可单独选择要合并的工作表（直接回车=全部），并支持按列关键词筛选数据行后再合并。模式选择菜单升级为 4 种模式并展示各自功能。 |
| v1.4.0 | 2026-08-08 | **支持批量粘贴文件输入**（参考 多表合并透视.py）：新增 `PATH_RE`/`split_line_paths`/`is_path_like`/`read_clipboard_paths`/`read_paths_batch`/`_PUTBACK` 预读缓冲；`ask_paths`/`ask_single_path`/`ask_optional_paths` 全部支持一次粘贴多个路径（Ctrl+V 多行粘贴、单行多个路径、输入 `c` 从剪贴板读取全部路径）；单文件步骤收到多余文件时自动暂存，供后续步骤直接消费，实现连续批量粘贴。 |
| v1.4.1 | 2026-08-08 | 批量粘贴体验修复：1) 各文件输入步骤识别到文件后打印「已识别 N 个文件（顺序即处理顺序）」列表，用户可确认；2) 剪贴板读取改为 pywin32（`win32clipboard`）优先、tkinter 后备；3) 输入 `c` 但剪贴板未读到路径时，明确提示「请先 Ctrl+C 复制文件」并允许重新输入，不再静默结束。 |
| v1.4.2 | 2026-08-08 | 全部模式的文件输入步骤提示统一改为「粘贴」引导（原为「拖入」）：所有涉及文件路径的提示均说明支持批量粘贴（Ctrl+V 多行、单行多路径）或输入 `c` 从剪贴板读取；模式选择菜单中的模式描述同步更新为「粘贴」表述。 |
| v1.5.0 | 2026-08-08 | **模式 4 改为多文件批量处理**：① 支持一次粘贴多个文件（Ctrl+V 多行 / 单行多路径 / 输入 `c` 从剪贴板），逐个处理并逐个列出处理结果；② 合并全部文件的工作表名称列出，选择一个统一应用到所有文件（按名称匹配）；③ 功能 2（保留列）先合并所有文件该工作表的列名，标注每列「全部文件都有 / 仅部分文件有」，一次选择保留列后统一按列名处理所有文件（不逐个选择处理）；④ 全部处理完后询问「是否继续处理其他模式」，输入 `y` 回到模式菜单继续，其他键结束。 |
| v1.5.1 | 2026-08-08 | 修复模式 3 合并大表时 Excel 内存不足崩溃（0x8007000E）：改为**流式分块读写**——源表按 2000 行分块读取、结果按 500 行小块写入（`write_merged_rows_in_small_chunks`），边读边写，不再把全部数据堆在 Python 内存，也不一次性赋值超大矩阵；`iter_range_rows_in_chunks` 增加 `chunk_rows` 参数支持按需覆盖块大小；删除已无引用的 `filter_rows_by_keywords`（筛选逻辑内联到分块处理）。 |
| v1.6.0 | 2026-08-11 | **模式 2 数据覆盖新增「只覆盖数据源列数」保护**：清除目标表旧数据由「整行删除」改为只清除前 `source_last_col`（数据源列数）列的内容，目标表第 N+1 列之后的内容（如新增的查找/公式列）完整保留，不被覆盖；写入逻辑天然只覆盖前 N 列。目标表工作表的选取逻辑保持不变。 |

## 3. 总体处理目标

脚本启动后，用户按顺序拖入以下文件：

1. 数据源表
2. `综合数据明细-新月份.xlsx`
3. `客户信息.xlsx`
4. `销售账号明细.xlsx`
5. `基础表-日期表.xlsx`

输入规则：

1. 每拖入一个文件后回车一次，表示继续输入下一个文件。
2. 当全部文件输入完成后，再额外回车一次。
3. 连续回车两次后，脚本正式开始处理。

处理过程中，脚本还会在指定节点继续等待用户拖入额外表格：

1. 在处理 `客户信息.xlsx` 时，需要额外拖入“新客户表”。
2. 在处理 `销售账号明细.xlsx` 时，可能需要终端交互确认或手工补录映射数据。

所有文件均在原路径直接覆盖保存，不另做副本。

## 4. 建议实现方式

由于需求中包含以下行为：

1. 依赖 Excel 公式下拉生效
2. 依赖“打开后保存”触发表内公式更新
3. 需要尽量保持原工作簿结构

因此脚本实现优先建议使用 Excel COM 自动化方案，例如：

1. `xlwings`
2. `pywin32`

不建议仅使用 `openpyxl` 作为唯一实现方案。

## 5. 详细流程

### 5.1 第 1 个文件：数据源表

处理规则：

1. 删除第 1 行。
2. 识别并删除最后一条汇总数据行。
3. 汇总数据行的识别依据：
   - 最后一行中，`R~AG` 列存在数据。
   - 只删除这一条汇总行，不误删正常数据。
4. 删除后，将剩余数据暂存，供后续写入 `综合数据明细-新月份.xlsx`。

### 5.2 第 2 个文件：综合数据明细-新月份.xlsx

处理规则：

1. 删除从第 2 行开始的全部旧数据，保留第 1 行表头。
2. 将“数据源表”暂存的数据，从 `A2` 开始写入。
3. 将 `J列` 的“客户代码”转换为数值类型。
4. 保存文件。

### 5.3 第 3 个文件：客户信息.xlsx

此步骤分为“客户代码检测”和“新客户表追加”两个阶段。

#### 5.3.1 客户代码检测阶段

数据来源：

1. 从 `综合数据明细-新月份.xlsx` 提取客户代码。
2. 客户代码来源列为 `J列`。

处理规则：

1. 将提取到的客户代码写入 `客户信息.xlsx` 的 `T列`。
2. 先对 `T列` 自身去重。
3. 再对 `T列` 与 `A列` 执行重复检测。
4. 检测目标不是输出 `A列` 数据，而是输出 `T列` 中“与 `A列` 不重复”的客户代码。
5. 将这些“`T列` 未重复客户代码”输出到终端，供用户查看。

#### 5.3.2 新客户表追加阶段

在输出 `T列` 未重复客户代码后：

1. 脚本暂停。
2. 等待用户拖入“新客户表”。
3. 将新客户表的数据追加到 `客户信息.xlsx` 现有数据末尾。

对新增区域执行以下处理：

1. 将新增区域的 `B列` 客户代码转换为数值类型。
2. 将 `Q列` 现有公式向下填充到新增区域末尾。
3. `Q列` 的业务含义是：基于 `B列` 的客户简称，对包含 `-` 的内容进行提取，保留 `-` 前面的部分。
4. `Q列` 已有公式，脚本只负责沿用并向下填充，不重写公式逻辑。

收尾规则：

1. 删除临时使用的 `T列`。
2. 保存 `客户信息.xlsx`。

### 5.4 第 4 个文件：销售账号明细.xlsx

此步骤分为“业务员检测”“自动映射填充”“异常交互确认”三个阶段。

#### 5.4.1 业务员检测阶段

数据来源：

1. 从 `综合数据明细-新月份.xlsx` 提取 `M列` 业务员。

处理规则：

1. 将提取到的业务员写入 `销售账号明细.xlsx` 的 `H列`。
2. 先对 `H列` 自身去重。
3. 再对 `H列` 与 `A列` 执行重复检测。
4. 仅将 `H列` 中“与 `A列` 不重复”的业务员，追加到 `A列` 末尾。
5. 不是把 `H列` 全部都追加，也不是仅做 `H列` 内部去重后直接追加。

#### 5.4.2 新增业务员字段填充规则

针对新增到 `A列` 的业务员数据：

1. `B列`：提取 `-` 后面的姓名。
2. `C列`、`D列`、`E列`：根据 `A列` 中 `-` 前面的前缀进行映射填充。

已确认映射表如下：

| A列前缀 | C列 部门 | D列 业务归属组织机构 | E列 业绩部门 |
| --- | --- | --- | --- |
| ZSZK- | 中山锦联 | 深圳锦联 | 华南 |
| ZSTH- | 中山锦联 | 深圳锦联 | 华南 |
| ZS- | 中山锦联 | 深圳锦联 | 华南 |
| XJ- | 深圳操作 | 深圳锦联 | 华南 |
| SZ4- | 深圳直客四部 | 深圳锦联 | 华南 |
| SZ3- | 深圳直客三部 | 深圳锦联 | 华南 |
| SZ2- | 深圳直客二部 | 深圳锦联 | 华南 |
| SZ1- | 深圳直客一部 | 深圳锦联 | 华南 |
| ST2- | 深圳同行二部 | 深圳锦联 | 华南 |
| ST1- | 深圳同行一部 | 深圳锦联 | 华南 |
| GZ1- | 广州直客一部 | 广州锦联 | 华南 |
| GT2- | 广州同行二部 | 广州锦联 | 华南 |
| GT1- | 广州同行一部 | 广州锦联 | 华南 |
| 14- | 销售十四部 | 事业三部 | 事业三部 |
| 13- | 销售十三部 | 事业三部 | 事业三部 |
| 12- | 销售十二部 | 事业三部 | 事业三部 |
| 11- | 销售十一部 | 事业三部 | 事业三部 |
| 10- | 销售十部 | 事业三部 | 事业三部 |
| 9- | 销售九部 | 事业三部 | 事业三部 |
| 8- | 销售八部 | 事业三部 | 事业三部 |
| 7- | 销售七部 | 事业三部 | 事业三部 |
| 6- | 销售六部 | 事业一部 | 事业一部 |
| 5- | 销售五部 | 事业一部 | 事业一部 |
| 4- | 销售四部 | 事业一部 | 事业一部 |
| 3- | 销售三部 | 事业一部 | 事业一部 |
| 2- | 销售二部 | 事业一部 | 事业一部 |
| 1- | 销售一部 | 事业一部 | 事业一部 |

#### 5.4.3 异常数据与终端交互

存在两类特殊情况。

第一类：前缀不在映射表中

1. 若 `A列` 的业务员值带有前缀，但前缀无法在已知映射表中命中，则将相关记录列出到终端。
2. 用户在终端输入对应的 `C/D/E` 数据。
3. 脚本据此回填。

第二类：纯姓名

1. 若 `A列` 只有姓名，没有前缀，则也视为异常情况。
2. 这类数据仍需先把姓名写入 `B列`。
3. `C/D/E` 由用户在终端输入。

确认机制：

1. 无论是“前缀未匹配”还是“纯姓名”，在脚本填充后，都要将对应新增记录的 `A/B/C/D/E` 输出到终端供用户确认。
2. 终端交互中：
   - 输入 `1` 表示确认当前结果无误。
   - 输入 `2` 表示用户准备手工修改。
3. 若用户输入 `2`，则等待用户在终端中提供修改后的数据。
4. 脚本以用户提供的新数据覆盖这些新增记录中的问题项。

收尾规则：

1. 在所有新增业务员记录确认完成后，删除临时 `H列`。
2. 保存 `销售账号明细.xlsx`。

### 5.5 第 5 个文件：基础表-日期表.xlsx

处理规则：

1. 打开 `基础表-日期表.xlsx`。
2. 直接保存。
3. 不修改任何单元格内容。

该步骤的目的是触发表内公式刷新。

## 6. 关键交互节点

脚本运行中至少包含以下交互：

1. 启动时拖入 5 个基础文件路径。
2. 输出 `客户信息.xlsx` 中 `T列` 检测得到的未重复客户代码。
3. 等待拖入“新客户表”。
4. 输出 `销售账号明细.xlsx` 中需人工确认或补录的业务员数据。
5. 接收用户输入的 `C/D/E` 数据。
6. 输出 `A/B/C/D/E` 给用户确认。
7. 接收 `1` 或 `2` 的确认指令。

## 7. 重要实现约束

1. 所有表格均在原路径覆盖保存。
2. 不生成副本。
3. 临时列仅用于检测，结束后必须删除：
   - `客户信息.xlsx` 删除 `T列`
   - `销售账号明细.xlsx` 删除 `H列`
4. 去重与重复检测是两个步骤，不能合并省略：
   - 先对临时列自身去重
   - 再对临时列与正式列做重复检测
5. 终端输出的重点是“临时列中检测出来的未重复数据”，不是正式列已有数据。

## 8. 当前确认状态

以下内容已确认：

1. `客户信息.xlsx` 中应输出的是 `T列` 检测后的未重复客户代码。
2. `销售账号明细.xlsx` 中 `H列` 必须先内部去重，再与 `A列` 做重复检测，最后只追加 `H列` 中未重复的业务员。
3. 当前阶段先产出理解文档，待确认后再进入脚本开发。

## 9. 模式 3 与模式 4 使用说明（v1.3.0 新增）

### 9.1 模式选择菜单

脚本启动后展示 4 种模式，每个模式附带功能说明：

- 模式 1：完整业务处理（原五表联动，不变）。
- 模式 2：数据覆盖——粘贴数据源 + 目标数据表，选择目标表要覆盖的工作表，将数据源前 N 列（N=数据源列数）内容覆盖到目标表，第 N+1 列之后的内容保留。
- 模式 3：合并表格——逐文件选择工作表 + 按列关键词筛选 + 合并。
- 模式 4：工作表处理——选择工作表后二选一（清除公式 / 保留列）。

### 9.2 模式 3：合并表格（升级）

交互顺序：

1. 依次拖入需要合并的表格文件，全部拖完后回车两次开始处理。
2. 询问是否做行筛选：
   - 输入 `y` 后继续输入筛选列编号（第 1 列 = 1）与筛选关键词（可多个，任一匹配即保留）。
   - 筛选规则：每个工作表的第 1 行（表头）始终保留；数据行中筛选列包含任一关键词的行被保留。
   - 若某张表的列数少于筛选列编号，则整张表不做筛选、全部保留。
3. 对每个文件列出全部工作表，输入编号选择（可多选，逗号分隔），直接回车表示选择全部。
4. 所有选中工作表的数据（可经过筛选）纵向追加，写入 `原文件名_合并结果.xlsx`。

### 9.3 模式 4：工作表处理（新增，v1.5.0 改为多文件批量）

交互顺序：

1. 粘贴要处理的表格文件（支持一次粘贴多个：Ctrl+V 多行、单行多路径、输入 `c` 从剪贴板读取）。
2. 选择功能：

   功能 1：复制全部数据并粘贴（清除公式）

   - 对每个选中工作表，复制整个已用区域，再原位粘贴为数值（`xlPasteValues`）。
   - 效果：公式被替换为计算后的值，格式、合并单元格、列宽均保留。
   - 适用：批量清除公式，避免后续搬运时公式失效或误算。

   功能 2：保留指定列，删除其余列

3. 合并全部文件的工作表名称列出（去重），选择一个工作表编号（回车默认第 1 个），该选择应用到所有文件（按名称匹配，无此工作表的文件跳过）。
4. 功能 2 会先合并所有文件该工作表的列名，列出统一清单并标注每列「全部文件都有 / 仅 X/N 个文件有」，供用户区分不同的表结构；一次选择要保留的列后回车，所有文件统一按所选列名处理（不逐个选择处理）。
5. 逐个处理文件，每个文件的处理结果（清除公式的单元格数 / 删除的列数）逐个列出。
6. 全部处理完后询问「是否继续处理其他模式」：输入 `y` 回到模式菜单继续，其他任意键结束。

### 9.4 实现要点

1. `ask_sheet_selection`：工作表多选解析，直接回车返回全部（模式 3 使用）。
2. `collect_all_sheet_names`：合并所有文件的工作表名称（去重），供模式 4 统一选择。
3. `ask_sheet_name_uniform`：单选工作表编号，回车默认第 1 个。
4. `ask_keep_columns_from_union`：合并列名清单 + 一致性标注 + 一次选择保留列。
5. `clear_formulas_by_paste_values`：Copy + PasteSpecial(-4163)，带 Excel 忙重试。
6. `keep_selected_columns_by_names`：按统一选择的列名批量保留，列数与模板一致时按位置、否则按名称匹配。
7. `filter_rows_by_keywords`：保留第 1 行表头，数据行按关键词包含匹配（模式 3 使用）。
8. 新增常量 `XL_PASTE_VALUES = -4163`。
9. `main` 增加循环：处理完一个模式后询问是否继续其他模式，输入 `y` 重新显示模式菜单。

### 9.5 批量粘贴文件输入（v1.4.0 新增）

所有需要输入文件路径的步骤（模式 1 的 5 个基础文件、模式 2 的源/目标表、模式 3 的多个合并文件、模式 4 的处理文件）都支持以下粘贴形态：

1. **Ctrl+V 多行粘贴**：在资源管理器中多选文件后 Ctrl+C，再到终端一次 Ctrl+V 粘贴多行路径，自动连续读取。
2. **单行多个路径**：`d:/a.xlsx d:/b.xlsx` 或 `"d:/a.xlsx" "d:/b.xlsx"`（引号包裹可含空格）。
3. **输入 `c`**：从剪贴板直接读取全部文件路径（`win32clipboard` 优先，`tkinter` 后备）。若剪贴板未读到路径，会明确提示「请先 Ctrl+C 复制文件」并允许重新输入。
4. **直接拖入**：原有单文件拖入方式保持不变。

实现机制：

- `PATH_RE = r'"([^"]*)"|(\S+)'`：优先匹配引号包裹路径，否则连续非空白。
- `read_paths_batch`：批量读取一行或多行，遇到空行结束本轮；非路径行暂存到 `_PUTBACK`。
- `_PUTBACK` 预读缓冲：单文件步骤（如模式 2 的源表）一次粘贴多个文件时，取第一个，其余暂存；下一个文件步骤直接从缓冲消费，无需重新输入。
- 上层统一 `_collect_valid_paths` 清洗去引号、校验存在性，无效路径打印提示并忽略。
- 结束确认：每批粘贴完成后按一次回车结束本轮；模式 3 的多个文件可多批粘贴后按回车结束。
